const { createPostmark, showPostmarkInfo } = require('./postmarks');
const { showLocations, showLocationInfo, showLocation } = require('./locations');

module.exports = {
    createPostmark, showLocations, showLocationInfo, showPostmarkInfo, showLocation
}